---
phase: 01-framework-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - justfile
  - Cargo.toml
autonomous: true

must_haves:
  truths:
    - "`just run` starts the Axum server"
    - "`just check` runs clippy without errors"
    - "`just fmt` checks formatting"
    - "`just test` runs all tests"
    - "`just build` compiles the project"
  artifacts:
    - path: "justfile"
      provides: "All required commands: build, run, test, check, fmt"
      contains: "build"
  key_links:
    - from: "justfile"
      to: "backend/Cargo.toml"
      via: "cargo commands reference the workspace"
      pattern: "cargo"
---

<objective>
Update the justfile to ensure all required commands (build, run, test, lint/check, format) work correctly with the new Axum backend, and add a `build` alias.

Purpose: INFR-06 requires common commands in justfile. The existing justfile is mostly correct but needs verification and minor updates after the Rocket->Axum migration.
Output: Working justfile where all commands succeed against the new Axum codebase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-framework-foundation/01-CONTEXT.md
@justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update justfile and verify all commands</name>
  <files>
    justfile
    Cargo.toml
  </files>
  <action>
**justfile** — Review and update:
The existing justfile already has most commands. Verify and update:

1. Add `alias b := build` if not present (the existing file has no `b` alias).
2. Ensure the `build` recipe exists: `build: cargo build` — already present.
3. Ensure `run` works: `run *args: cargo run {{ args }}` — already present. Verify it starts the Axum server.
4. Ensure `check` works: `cargo clippy` — already present.
5. Ensure `fmt` works: `cargo +nightly fmt` — already present.
6. Ensure `test` works: `cargo test` — already present.
7. Ensure `nextest` works: `cargo nextest run` — already present.
8. Ensure `compile-test` works: `cargo test --no-run` — already present.

The justfile looks good as-is. Only add `alias b := build` if missing.

**Cargo.toml (workspace root):**
If the root Cargo.toml is a workspace config, ensure it includes the backend member. Check if it needs updating. If it's just `[workspace] members = ["backend"]`, it should be fine.

**Verification pass:**
Run each command and confirm it works:
- `just build` — compiles without errors
- `just check` — clippy passes (fix any warnings if reasonable)
- `just fmt` — formatting check passes (run `just fmt -w` to fix if needed)
- `just test` — tests pass (including the integration tests from Plan 02, if that ran first; otherwise just compilation tests)
- `just run` — starts the server (ctrl+c to stop)

If any command fails, fix the issue (likely clippy warnings from the new code or formatting issues).
  </action>
  <verify>
Run each command sequentially:
1. `just build` exits 0
2. `just check` exits 0 (no clippy errors)
3. `just fmt` exits 0 (formatting correct)
4. `just test` exits 0
5. `just ct` exits 0 (compile tests)
6. `just run &` starts server, `curl http://localhost:8000/health` returns 200, then kill the background process
  </verify>
  <done>
All justfile commands (build, run, test, check, fmt, nextest, compile-test) work correctly with the Axum backend. `just run` starts the Axum server. `just check` passes clippy. `just fmt` passes formatting. `just test` runs all tests.
  </done>
</task>

</tasks>

<verification>
1. `just build` — exit 0
2. `just check` — exit 0
3. `just fmt` — exit 0
4. `just test` — exit 0
5. `just run` — server starts, /health returns 200
</verification>

<success_criteria>
- All five core justfile commands succeed: build, run, test, check, fmt
- Server starts via `just run` and responds to requests
- No clippy errors from `just check`
</success_criteria>

<output>
After completion, create `.planning/phases/01-framework-foundation/01-03-SUMMARY.md`
</output>
